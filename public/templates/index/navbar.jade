
nav.navbar.navbar-default.navbar-fixed-top(role = "navigation")

  .navbar-header
    a.navbar-brand(href = "/") #{parentDict.caption}
    //- a.navbar-brand(href = "#") #{name}

  //- ul.nav.navbar-nav
  //-   li.active
  //-     a(href = "#") #{parentDict.caption}

  form.navbar-form.navbar-left(data-view = "search", data-search-dict = "#{parentDict.sid}", role = "search")
    .form-group
      input.form-control(type = "text", placeholder = "#{gettext('Search')}")

  .pull-right
    ul.nav.navbar-nav
      li
        a(href = "#auth/logout") 
          i.glyphicon.glyphicon-log-out
          &nbsp;
          span #{gettext('Logout')}
